import examples.std

struct Worker {
    name string
    next voidptr
}

fun main() int {
    let worker pointer@Worker = examples.std.malloc(Worker)

    worker.name = "hello"

    examples.std.print(worker.name)
}